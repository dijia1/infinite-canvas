import { PublicImageManager } from "./public-image-manager";

export default function AdminAssetsPage() {
    return (
        <main className="p-6">
            <PublicImageManager />
        </main>
    );
}
